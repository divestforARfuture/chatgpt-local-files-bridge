# Sample document

This file exists to verify that ChatGPT can search for and read Markdown files inside the approved workspace.

The bridge is deliberately limited to a single workspace and binds only to `127.0.0.1`.
